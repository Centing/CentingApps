<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <FrameLayout
        android:id="@+id/frame_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottomBar">

    </FrameLayout>

    <me.ertugrul.lib.SuperBottomBar
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        app:sbb_menu="@menu/bottom_nav"
        android:layout_alignParentBottom="true"
        app:sbb_backgroundColor="@color/white"
        app:sbb_activeColor="@color/colorPrimaryDark"/>

    <include layout="@layout/layout_false_login"
        android:id="@+id/divStateLogin"
        android:visibility="gone"/>
</RelativeLayout>