<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CentingApps" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CentingApps" parent="Base.Theme.CentingApps">
        <item name="android:textViewStyle">@style/AppTextStyle</item>
        <item name="materialButtonStyle">@style/AppMaterialButtonStyle</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--        <item name="android:textColorPrimary">@color/white</item>-->
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
    </style>
    <style name="AppTextStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/poppins_regular</item>
        <item name="android:textSize">12dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AppMaterialButtonStyle" parent="Widget.Material3.Button">
        <item name="fontFamily">@font/poppins_regular</item>
        <item name="android:textSize">12dp</item>
        <!--        <item name="cornerRadius">32dp</item>-->
    </style>


    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
    </style>

    <style name="TEXT">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Widget.App.Switch" parent="Widget.Material3.CompoundButton.MaterialSwitch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <style name="ThemeOverlay.App.Switch" parent="">
        <item name="colorPrimary">@color/greyMudaRBA</item>
    </style>


    <!-- Gaya khusus untuk TextInputLayout -->
    <style name="Widget.TextInputLayout.Custom" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/colorTransparent</item> <!-- Ubah warna outline -->
        <item name="android:textColorHint">@color/colorPrimaryDark</item> <!-- Warna hint -->
        <item name="hintTextAppearance">@style/TextAppearance.CustomHint</item> <!-- Gaya hint -->
    </style>

    <!-- Gaya khusus untuk hint text -->
    <style name="TextAppearance.CustomHint" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">10sp</item> <!-- Ubah ukuran teks hint -->
        <item name="android:textStyle">bold</item> <!-- Ubah gaya teks hint ke tebal -->
        <item name="android:fontFamily">@font/poppins_bold</item> <!-- Ubah gaya teks hint ke tebal -->
        <item name="android:textColor">@color/colorPrimaryDark</item> <!-- Ubah warna teks hint -->
    </style>

    <style name="DatePickerDialogStyle" parent="@android:style/Theme.Material.Light.Dialog">
        <item name="android:colorControlActivated">@color/colorPrimaryDark</item> <!-- Untuk bulan yang dipilih -->
        <item name="android:textColorPrimary">@color/colorPrimaryDark</item> <!-- Untuk teks dan angka -->
        <item name="android:colorAccent">@color/colorPrimaryDark</item> <!-- Untuk header -->
        <item name="android:buttonBarButtonStyle">@style/DialogButtonStyle</item> <!-- Untuk header -->
    </style>

    <style name="DialogButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorPrimaryDark</item> <!-- Warna teks tombol "OK" -->
    </style>


</resources>